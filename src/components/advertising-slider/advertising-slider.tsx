import styles from "./advertising-slider.module.css";

const AdvertisingSlider = () => (
  <section className={styles.main_container}>
    <span className={styles.message}>ENVIOS A TODO EL PAIS</span>
  </section>
);

export default AdvertisingSlider;
